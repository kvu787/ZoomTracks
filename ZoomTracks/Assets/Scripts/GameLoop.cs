using UnityEngine;

public class GameLoop : MonoBehaviour {
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start() {
        Debug.Log($"{nameof(GameLoop)}.Start called");
    }

    // Update is called once per frame
    void Update() {
    }
}
